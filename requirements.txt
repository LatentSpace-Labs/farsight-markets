# Farsight Markets — Dependencies
# Install: pip install -r requirements.txt

# HTTP & WebSocket clients
httpx>=0.27.0
websockets>=12.0

# Data validation
pydantic>=2.0.0
pydantic-settings>=2.0.0

# GraphQL (for Goldsky subgraph queries — on-chain positions/PnL)
gql[httpx]>=3.5.0

# UUID generation
uuid-utils>=0.6.0

# API server (optional — only needed for `serve` command)
fastapi>=0.115.0
uvicorn>=0.30.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0